# CaveSurveyTool Backend Configuration
# Copy this file to .env and fill in your values
# DO NOT commit the .env file to version control

# ============================================================
# REQUIRED CONFIGURATION
# ============================================================

# Application Settings
APP_NAME=Cave Survey API
DEBUG=false

# Database Configuration (REQUIRED)
# For local development with PostgreSQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/cave_survey
# For Render (set automatically):
# DATABASE_URL=postgresql://user:password@host:5432/database
DATABASE_URL=postgresql://user:password@localhost:5432/cave_survey

# Security: JWT Secret Key (REQUIRED)
# Generate a secure secret key with: openssl rand -hex 32
# This MUST be set for the application to start
SECRET_KEY=your-secret-key-here-use-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=30
ALGORITHM=HS256

# ============================================================
# AWS S3 CONFIGURATION (Required for file storage)
# ============================================================
AWS_ACCESS_KEY_ID=your-aws-access-key
AWS_SECRET_ACCESS_KEY=your-aws-secret-key
AWS_DEFAULT_REGION=us-east-1
S3_BUCKET_NAME=your-bucket-name
S3_PUBLIC_READ=false
PRESIGN_EXPIRE_SECS=3600

# ============================================================
# CORS Configuration
# ============================================================
# Comma-separated list of allowed origins
# For local development:
# ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
# For production, add your frontend URL:
# ALLOWED_ORIGINS=https://your-frontend.netlify.app,http://localhost:5173
ALLOWED_ORIGINS=http://localhost:5173

# ============================================================
# DATADOG MONITORING (Optional but recommended for production)
# ============================================================
DATADOG_API_KEY=
DATADOG_APP_KEY=
DD_SERVICE=cave-survey-api
DD_ENV=production
DD_VERSION=1.0.0

# ============================================================
# NOTES
# ============================================================
# 1. Generate SECRET_KEY with: openssl rand -hex 32
# 2. Never commit .env file to git (it's in .gitignore)
# 3. For Render deployment, set these in the Render dashboard
# 4. Minimum required: DATABASE_URL, SECRET_KEY
# 5. For full functionality, also set AWS S3 credentials
